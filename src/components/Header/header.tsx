import { Component } from 'react';
import Search from '../Search/search';

export class Header extends Component {
  render() {
    return (
      <header>
        <Search />
      </header>
    );
  }
}
